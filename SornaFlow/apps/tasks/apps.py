from django.apps import AppConfig


class TasksConfig(AppConfig):
    name = 'apps.tasks'              # Full Python path of the tasks app
    verbose_name = 'مدیریت وظایف'    # Human‑readable name shown in Django admin
